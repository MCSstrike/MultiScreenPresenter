services:
  multiscreen-server:
    build:
      context: ./server
    container_name: multiscreen-server
    ports:
      - "3000:3000"
    environment:
      - PORT=${PORT:-3000}
      - ORIGIN=${ORIGIN:-*}
    restart: unless-stopped
